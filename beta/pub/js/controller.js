/*
 * The most beautiful JS file, controlls everything on
 * startup so it's all presented beautifully.
 */

  $(window).on('load resize', function(){
    // $('#content').height( $(window).height() - $('#nav').outerHeight(true) );
  });
